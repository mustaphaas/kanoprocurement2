// Firebase disabled: running in localStorage-only mode
export const hasFirebaseConfig = false as const;
export const auth = null;
export const db = null;
export const storage = null;
export default null;
